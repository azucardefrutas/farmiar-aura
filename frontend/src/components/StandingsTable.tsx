import type { Placement, Standing } from '../types'

export function StandingsTable({ standings, placements }: { standings: Standing[]; placements: Placement[] }) {
  return <div className="space-y-6">{placements.length > 0 && <div className="podium-grid">{placements.filter((item) => item.place <= 3).map(({ place, contestant }) => <article key={contestant.id} className={`podium-card place-${place}`}><span>{place}º lugar</span>{contestant.photoUrl ? <img src={contestant.photoUrl} alt="" width="64" height="64" /> : <div className="podium-avatar">{contestant.name.slice(0, 2).toUpperCase()}</div>}<strong>{contestant.name}</strong><small>{contestant.program}</small></article>)}</div>}<div className="standings-wrap"><table className="standings-table"><thead><tr><th>Pos.</th><th>Participante</th><th>PJ</th><th>PG</th><th>Votos</th><th>Aura</th></tr></thead><tbody>{standings.map((standing) => <tr key={standing.contestant.id}><td>{standing.placement ? `${standing.placement}º` : '—'}</td><td><strong>{standing.contestant.name}</strong><small>{standing.contestant.program}</small></td><td>{standing.played}</td><td>{standing.wins}</td><td>{standing.votes}</td><td>{standing.aura.toLocaleString('es-MX')}</td></tr>)}</tbody></table></div></div>
}
